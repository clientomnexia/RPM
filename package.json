{
    "name": "rpm-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "npm run build:frontend && npm run build:admin",
        "build:frontend": "cd frontend && npm install && npm run build && cp -r dist/* ../dist/",
        "build:admin": "cd admin && npm install && npm run build && mkdir -p ../dist/admin && cp -r dist/admin/* ../dist/admin/",
        "dev:backend": "nodemon api/index.js",
        "dev:frontend": "cd frontend && npm run dev",
        "dev:admin": "cd admin && npm run dev",
        "seed": "node api/seeder.js"
    },
    "dependencies": {
        "bcryptjs": "^3.0.3",
        "cors": "^2.8.6",
        "dotenv": "^16.6.1",
        "express": "^4.22.1",
        "jsonwebtoken": "^9.0.3",
        "mongoose": "^8.23.0",
        "mongoose-unique-validator": "^5.0.2",
        "multer": "^2.1.0"
    },
    "devDependencies": {
        "nodemon": "^3.1.14"
    }
}